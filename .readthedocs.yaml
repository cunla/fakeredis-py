version: 2
build:
  os: "ubuntu-24.04"
  tools:
    python: "3.14"
  jobs:
    install:
      - pip install --upgrade pip
      - pip install --group 'documentation'

mkdocs:
  configuration: mkdocs.yml
  fail_on_warning: false
